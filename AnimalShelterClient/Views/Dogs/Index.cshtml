@{
  ViewData["Title"] = "Home Page";
}

<h1>All Dogs:</h1>

@if (@Model.Count == 0)
{
  <h3>No Dogs have been added yet!</h3>
}

<ul>
  @foreach (Dog dog in Model)
  {
    <li>@Html.ActionLink($"{dog.Name}", "Details", new { id = dog.DogId })</li>
    <br>
  }
</ul>