@{
  ViewData["Title"] = "Home Page";
}

<h1>All Cats:</h1>

@if (@Model.Count == 0)
{
  <h3>No Cats have been added yet!</h3>
}

<ul>
  @foreach (Cat cat in Model)
  {
    <li>@Html.ActionLink($"{cat.Name}", "Details", new { id = cat.CatId })</li>
    <br>
  }
</ul>